@model AccountViewModel

<div class="account">
    <div class="account__image"></div>
    <div class="account__name">
        <span class="account__name-accent">@Model.DisplayName?[0]</span>
        <span class="account__name-other">@Model.DisplayName?.Substring(1, Model.DisplayName.Length - 1)</span>
    </div>
</div>